name: Say Hello

on: [push]

jobs:
  greet:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Welcome Message
        run: echo "🚀 Hello Shiva! GitHub Actions is working!"
